# PII 마스킹 계약 실측 — 2026-07-25 (하네스, UPSTAGE 미설정 → 실 Solar 호출 0건)

입력(테스트용 가짜 PII): "... 제 이메일은 demo@example.com 이고 연락처는 010-9876-5432 입니다."

응답:
- notice: "Solar API 키가 없어 규칙 기반 샘플 결과를 표시합니다. 이메일·전화번호(으)로 보이는 정보는 가리고 분석했어요."
- masked: ["이메일", "전화번호"]
- 응답 JSON 전체에 원문 PII 포함 여부: False ([이메일]/[전화번호]로 대체)
- 인용 검증(experience.includes(quote))은 마스킹 텍스트 기준으로 유지되어 claims 정상 생성

## 마스킹 안내 UI 실측 (2026-07-25 보완, masking-notice-ui.png)
- STEP2 explain-strip에 "…가리고 분석했어요" 고지 표시: true
- 화면(strip+인용문)에 원문 PII 노출: false
- 인용문에 마스킹 토큰([이메일]/[전화번호]) 표시: true
- STEP2 legend 문구: "입력 문장을 근거로 인용해요 · 개인정보가 감지되면 가려서 표시돼요"
  (개인정보 없는 입력은 기존 exact-quote 원칙 그대로 — 기존 테스트 유지로 검증)
