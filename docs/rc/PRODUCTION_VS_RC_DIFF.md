# 운영(1차) vs RC 차이 감사 (실측, 2026-07-25)

- 운영: 버전 `cece759c`(main `19c8c48` 빌드) — https://gapproof.forblune.com 실측
- RC: `rc/professor-feedback` `c1afc34` (PR #63, main 대비 14커밋·84파일 ±4,416/−947)

## 라우트 차이 (운영 실측 상태코드)

| 라우트 | 운영(1차) | RC | 사용자 영향 |
|---|---|---|---|
| `/` | 200 — **게이트+데모**(코드 없으면 아무것도 못 봄) | **공개 홈 10섹션**(무코드) | 기존 공유 링크(`/`)가 게이트 대신 제품 스토리로 열림 — 의도된 개선. 데모로 가려면 홈 CTA→/demo |
| `/demo` | **404** | 게이트+5단계 데모(+`?sample=1` 무코드 샘플) | 신규 진입점 — 안내 필요(데모 코드 공지에 URL 병기) |
| `/why` `/who` | 404 | 200 신설 | 신규 |
| `/privacy` `/terms` | 404 | 200 신설(v0.1 초안) | 신규 — 푸터에서 접근 |
| `/about` `/guide` `/how-it-works` `/technology` | 200 | 200(용어 통일·중복 정리·내비 7항목) | 저위험 개편 |
| manifest·robots·sitemap·자산 | 200 | 200(+sitemap에 /why /who /privacy /terms) | 무해 |

## 기능 차이 (운영에 없는 RC 기능)

| 기능 | 운영(1차) | RC |
|---|---|---|
| 코드 없는 샘플 체험 | 없음 | 있음(실 Solar 0회·3중 표시·draft 불간섭) |
| draft 저장·복원(만료·재시작 복귀) | **없음 — 재시작 시 입력 소실(P0)** | 있음(기기 내 localStorage, 잠금/새 분석 시 소거) |
| 입력 | 기본 예시 주입·3,000자 | 기본값 없음·**10,000자**(절삭 금지)·장문 예시 3종 |
| 분석 결과 | V1(주장·인용·질문) | **V2**(사실 상태·행동·신호·근거 강도·과장 위험·직업 가설(단정 차단)·작은 실험) |
| 모델 선택 | native select | 카드 다이얼로그([공식]/[자체]·평가 중) |
| AI 대화 가져오기·TXT/MD 첨부 | 없음 | 있음(업로드 없음) |
| 파괴적 작업 문구 | 기록 삭제/데모 잠금 | 새 분석 시작하기/데모 나가기(+확인창 개편) |
| 회원가입·로그인·초대 코드·usage ledger | 없음 | **UI 없음(동일)** — 로컬 스키마·RLS 설계만 저장소에 존재, 운영에서 활성화되지 않음 |
| 실제 Solar 연결 | 서버 키 존재 시 실연결(운영 확인됨) | 동일 코드 경로(시크릿 3종 그대로 재사용 — 추가 등록 불필요) |

## 서버 계약 변화(호환성)

- `/api/analyze`: 최대 길이 3,000→10,000(413 문구 변경), 응답 claims에 **선택적 V2 필드 추가**(구 필드 유지 — 하위 호환), 401 안내 문구 변경
- `/api/gate`: 오류 문구 용어 변경("데모 코드") — 계약 구조 동일
- 쿠키·시크릿·바인딩·보안 헤더: **무변경** (기존 세션 쿠키는 RC에서도 유효 — 배포 시 재로그인 불필요)

## 배포 전후 사용자 영향 요약

1. 다운타임 0(원자 전환), 세션 유지, 시크릿 재등록 불필요
2. `/` 의미 변화가 가장 큰 가시 변화 — 심사·공유 안내문에 "데모는 /demo" 병기 권장
3. 구버전 탭을 연 채로 배포를 맞은 사용자: 다음 내비게이션에서 새 번들 로드(주의: 열린 SPA 상태는 페이지 이동 시 초기화 — RC부터는 draft가 이를 보완)
4. localStorage `gp_draft_v1`은 1차에 없던 키 — 마이그레이션 이슈 없음
