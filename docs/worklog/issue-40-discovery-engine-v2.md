# Issue #40 — Gate 4: 삶의 경험 발견 엔진 V2

- Issue: #40 · 브랜치 `feat/40-discovery-engine-v2`(base rc `f39720f`) · 작업일 2026-07-25

## 구현

- **`app/lib/engine-v2.ts` 신설**(순수 검증 모듈 — MindHub 추출 검증+폴백 패턴의 스키마 재정의): ClaimV2 = skill·quote·**factStatus(확인됨/부분 확인/계획·관심)**·context·behaviors(≤3)·**signals(반복/책임/문제 해결/몰입)**·**evidenceStrength(강/보통/약)**·**overclaimRisk**·question·**jobHypotheses(≤2, title+reason+missing 필수)**·smallStep
- **단정 금지 강제(코드 레벨)**: ①인용 정합 강화(공백 정규화·5자 미만 조각 거부·부분 변형 거부) ②가설은 reason/missing 없으면 폐기 ③판정 어휘(적합·적성·전문가·보장 등) 포함 가설 폐기 ④열거형 밖 값은 과장 방지 방향 강등(factStatus→부분 확인, strength→약함) ⑤규칙 기반 폴백은 가설 생성 금지
- **프롬프트 V2**: 돌봄·게임·커뮤니티·SNS·휴식기 명시 포함, "게임→코치/SNS→마케터 단정 금지"·"직업은 근거 있는 가설로만" 규칙 명문화
- **STEP2 카드 확장**: 상태·근거 강도·신호 배지, 상황·확인된 행동, 과장 주의(경고 톤), **직업 가설(판정 아님 라벨+(가설) 표기+부족한 증거)**, 이번 주 작은 실험 — 전부 선택 렌더(구버전·규칙 폴백 호환)
- 샘플 여정 fixture에 V2 필드 부여(원문 정합 유지)

## 검증

- **신규 단위 7종** `tests/engine-v2.test.mjs`(npm test 편입 → **28/28**): 13종 경험 fixture(게임·SNS·돌봄·육아·알바·독학·휴학·공백·실패·중단·커뮤니티·개인사업·혼합) 인용 생존, 환각 폐기, 공백 흡수/변형 거부, 가설 차단 3경로, 강등 규칙, 상한(3·3·2), 깨진 응답 안전 — **실제 모델 호출 0**
- e2e v2-ui **8/8**(390px): 배지·행동·과장 주의·가설 라벨·작은 실험 렌더 + overflow 0 (`docs/evidence/issue-40/`)
- lint 레거시 4 외 0(미사용 타입 제거) · tsc 레거시 2 외 0 · diff OK

## 알려진 한계

- 실제 Solar 응답의 V2 필드 충실도는 Gate 10 운영 1회 검증에서 확인(스키마 미준수 시 안전 강등이 이미 동작) · STEP3/4 카드의 V2 반영(가설 요약 등)은 범위 외(현행 유지)
