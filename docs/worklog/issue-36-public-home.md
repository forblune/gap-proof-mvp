# Issue #36 — Gate 2a: 공개 홈페이지(10섹션)와 탐색 구조

- Issue: #36 · 브랜치 `feat/36-public-home`(base rc `07eec50`) · 작업일 2026-07-25

## 구조 결정(자율·기록)

- **데모를 `/demo`로 이동**(`git mv app/page.tsx app/demo/page.tsx`, 게이트·5단계 흐름 무변경) — `/`는 무게이트 공개 홈. 데모 진입 정책(#6 전체 게이트)은 /demo에서 그대로 유지
- 모바일 내비는 드로어 대신 **랩 내비**(기존 info-nav 44px 재사용) — JS 없이 키보드·SR 접근성 기본 보장, RC 범위 최소화
- `/demo`는 sitemap에서 의도적 제외(게이트 뒤), 홈·정보 페이지는 유지
- 홈 CTA 2종은 현 단계에서 모두 /demo로 연결하고 "데모 코드로 입장" 정직 고지 — Gate 2b(무코드 샘플)에서 샘플 CTA를 분리(어느 커밋에서도 미구현 약속 없음)
- /why·/who 링크는 Gate 2c 신설 전까지 기존 /about으로 연결(죽은 링크 0 원칙)

## 홈 구성(J·K·Q 지시 반영)

Hero(지정 문구: "경력이라고 생각하지 않았던 경험에서 다음 가능성을 발견하세요." + 지정 설명 + CTA 2종) → 왜 필요한가(3카드) → 누구를 위한가(6대상) → 가능한 경험(칩 8종+마스킹·무저장 고지) → 작동 방식(4단계·사용자 확정 강조) → **가상 사례 3종**(게임·커뮤니티/가족 돌봄/알바·SNS·취미 혼합 — 각 원문·확인된 행동·역량 후보·원문 근거·확인 질문·과장 위험·직업 가설·작은 실험, "실존 인물 아님" 고지) → 모델 3종 요약 → 기술·안전 4카드 → **현재/다음 단계 구분**(미구현은 "준비 중"으로만) → 최종 CTA + 판정 아님 고지. 푸터 내비 5링크.

## 변경 파일

- `app/demo/page.tsx`(이동+임포트 경로), `app/page.tsx`(신규 서버 컴포넌트·홈 메타데이터), `app/globals.css`(+.home-* ~60줄, 기존 토큰만), `app/components/info-shell.tsx`(CTA·브랜드 링크 → /demo·홈), `tests/rendered-html.test.mjs`(게이트 계약 → /demo, 공개 홈 신규 테스트 1종, 데모 소스 경로 3곳)

## 검증

- `npm test` **21/21**(신규: 홈 공개 스토리 10문구+게이트/폼 비노출+데모 링크) · lint 레거시 4 외 0 · tsc 레거시 2 외 0 · diff-check OK
- 홈 5뷰포트 overflow 0·console 0(로컬 manifest CORS 아티팩트 제외 — #35 기록 참조) · `/demo` 게이트 SSR 유지 · 정보 페이지 CTA→/demo (`docs/evidence/issue-36/`)
- 30초 이해 7문항 콘텐츠 리뷰: ①무엇=Hero+작동 방식 ②누구=누구를 위한가 ③어떤 경험=칩 섹션 ④AI/사용자=작동 방식 3단계 ⑤결과=현재 제공+사례 ⑥개인정보·AI 한계=기술·안전+판정 고지 ⑦현재/예정=현재와 다음 단계 — 전 문항 홈 단독 응답 가능(제목 스캔만으로도 섹션 eyebrow가 답 지시)

## 알려진 한계

- /why·/who 미신설(Gate 2c) — 임시로 /about 연결 · 샘플 무코드화는 Gate 2b · Lighthouse·200% 확대는 Gate 10 일괄
