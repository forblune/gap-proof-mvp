# Lighthouse 90/95 개발일지

- 목표: `/`, `/demo?sample=1`, `/how-it-works`, `/technology` 4개 페이지의 모바일 Lighthouse Performance ≥90, Accessibility·Best Practices·SEO ≥95(각 3회 측정 중앙값). main 병합·운영 배포 없이 Draft PR까지.
- 시작 조건 확인: `origin/main`=`644f8c890480ddfb8624a2951a77d67d1a9d3588`(예상과 일치), `git status` clean, PR #77 병합 확인됨. 새 브랜치 `perf/lighthouse-90`을 `origin/main`에서 생성.
- 측정 환경: macOS 로컬, Node v26.3.1, Lighthouse CLI(devDependency, `node_modules/.bin/lighthouse`), `formFactor: mobile`, `throttlingMethod: simulate`(150ms RTT · ~1.6Mbps down · 4x CPU slowdown, `configSettings`로 실측 확인), 화면 412×823 @1.75x(Moto G 유사).

## Loop 1 — 기준선 측정 (OBSERVE)

**1차 시도 — `vinext start`(port 3000) 기준**: `npm run build && npm run start` 후 4페이지×3회 측정.

| 페이지 | Performance(3회) | 중앙값 | Accessibility | Best Practices | SEO | LCP |
|---|---|---|---|---|---|---|
| `/` | 87,89,90 | **89** | 100 | 96 | 100 | 2948ms |
| `/demo?sample=1` | 82,86,84 | **84** | 96 | 96 | 100 | 3460ms |
| `/how-it-works` | 90,90,91 | **90** | 100 | 96 | 100 | 2862ms |
| `/technology` | 90,90,90 | **90** | 100 | 96 | 100 | 2856ms |

Accessibility·Best Practices·SEO는 이미 전부 ≥95. Performance만 미달(home -1, demo -6, how-it-works/technology는 3회 중 최저 90으로 경계선 — "한 번만 나온 최고점으로 통과 처리 금지" 원칙상 안전 마진 부족).

**근거 조사(코드 확인, 추측 아님)**:
- `curl -D-`로 응답 헤더 확인: `/assets/index-*.css`(63KB)·JS 번들(framework 190KB + index 81KB, demo는 +page 79KB)이 `Content-Encoding` 헤더 없이 **무압축**으로 서빙됨(HTML 문서만 brotli 압축).
- HTML `<head>`: CSS `<link rel="stylesheet">`가 렌더 블로킹으로 최상단에 위치, 뒤이어 7개 `<link rel="modulepreload">`가 같은 대역폭을 경쟁.
- Lighthouse 자체 audit: `unused-css-rules`(25% · 16KB), `unused-javascript`(framework 44%, index 66%), `total-byte-weight` 351~421KB.
- FCP≈LCP≈Speed Index(≈2.9s, 페이지 간 거의 동일)이면서 TBT는 21~36ms로 매우 낮음 → 병목이 CPU가 아니라 **스로틀링된 네트워크에서 다운로드해야 하는 바이트 총량**임을 시사.

**재현·원인 확정**: `vinext start`는 vinext 자체의 범용 Vite 프리뷰 서버로, 이 프로젝트의 실제 배포 대상인 Cloudflare Workers(`wrangler.jsonc`의 `assets` 바인딩)와 무관하다. 실제 운영은 Workers Assets 바인딩이 정적 자산을 자동 압축해서 서빙한다(Cloudflare 표준 동작). **로컬 성능 테스트 서버 선택 자체가 측정 오류의 원인**이라는 가설을 세우고, `npx wrangler dev --port 3001`(실제 `workerd` 런타임을 로컬에서 그대로 구동, 같은 `dist/` 빌드 산출물과 `wrangler.jsonc` 설정 사용)로 교체해 검증.

**검증**: `curl -D-`로 `wrangler dev`가 서빙하는 CSS·JS 자산에 `Content-Encoding: gzip` + `Cache-Control: public, max-age=31536000, immutable` + `ETag`가 있음을 확인(운영과 동일한 Assets 바인딩 동작).

**2차 측정 — `wrangler dev`(port 3001) 기준(정정된 기준선)**:

| 페이지 | Performance(3회) | 중앙값 | Accessibility | Best Practices | SEO | LCP |
|---|---|---|---|---|---|---|
| `/` | 98,99,99 | **99** | 100 | 96 | 100 | 1728ms |
| `/demo?sample=1` | 98,98,98 | **98** | 96 | 96 | 100 | 2002ms |
| `/how-it-works` | 100,99,99 | **99** | 100 | 96 | 100 | 1679ms |
| `/technology` | 99,100,99 | **99** | 100 | 96 | 100 | 1686ms |

`formFactor: mobile`·동일 throttling 파라미터를 `configSettings`로 재확인(“쉬운 서버로 바꿔서 점수만 올린” 것이 아님을 실측으로 증명). **4개 페이지 전부 이미 모든 기준을 충족.** 이 시점에서 애플리케이션 코드에는 아무 변경도 없었다 — 발견된 것은 측정 인프라의 결함이었다.

**KEEP**: 측정 방법론 교정(`wrangler dev` 사용)을 공식 기준선으로 채택. `vinext start`는 정적 자산 무압축이라는 알려진 한계가 있으므로 향후 로컬 성능 측정에 사용하지 않는다.

## 팀 구성 및 1차 독립 검토

리드는 설치된 Agency Agents 중 "Frontend Developer"를 Performance Engineer 역할로 선택(전용 "Performance Engineer" 에이전트 없음, 번들·렌더링·네트워크 분석이 가장 근접).

- **Lead(Frontend Developer, Performance Engineer 역할)**: `wrangler.jsonc`의 실제 Assets 바인딩과 `package.json`의 `start` 스크립트를 직접 대조해 "vinext start는 wrangler 설정과 무관"함을 재확인. `wrangler dev`가 배포 대상에 가장 가까운 로컬 프록시이며, 이 저장소에서 Rate Limiting/Images 바인딩은 4개 측정 페이지의 렌더링 경로에 있지 않음(둘 다 `/api/gate`·`/api/analyze`용, 렌더 블로킹 아님)을 확인. gzip만 확인되고 brotli는 미확인이라 실제 운영은 이보다 같거나 더 나을 가능성이 높다고 판단(로컬 수치가 보수적). **판정: 앱 코드 변경 불필요, 진행 준비 완료.**
- **SEO Specialist**: `/demo`가 클라이언트 컴포넌트(`"use client"`)라 자체 metadata를 export할 수 없어 루트 layout의 `canonical: "/"`을 그대로 물려받아 **`/demo`의 canonical이 홈페이지를 가리키는 실제 버그**를 발견(Lighthouse SEO 점수는 canonical의 "존재"만 확인하고 "적절성"은 확인하지 않아 100점으로 가려져 있었음). heading 구조·robots.ts·sitemap.ts는 문제 없음. **판정: KEEP WITH MINOR NOTES** — canonical 수정 권고(부가로 noindex도 제안했으나 아래 Loop 2에서 되돌림).
- **Accessibility Auditor**: `/demo?sample=1`의 Accessibility 96(다른 페이지는 100)의 원인을 Lighthouse JSON에서 직접 추적 — `color-contrast` 감사가 STEP0의 선택(옵션) 동의 체크박스 안내문(`<small>`)에서 실패(4.44:1, 필요 4.5:1, 0.06 부족). 원인을 `app/globals.css:87`의 `.check-row.optional { opacity: .82; }`로 정확히 특정. **추가로 기존 axe-core 테스트 스위트의 구조적 사각지대를 발견**: `tests/e2e/accessibility.spec.ts`는 `/demo`(게이트 화면)만 스캔하고, 다른 e2e 파일들은 전부 `reachLookIntoStep()` 헬퍼로 STEP0을 지나친 뒤에만 axe를 실행해 이 위반이 한 번도 잡힌 적이 없었음. **판정: KEEP WITH MINOR NOTES** — 명도 대비 수정 + STEP0 axe 커버리지 추가 권고.

## Loop 2 — 수정 (실제 코드 문제 2건)

리드 승인: SEO/Accessibility 두 팀원이 각각 발견한 실제 버그(Lighthouse 점수에는 안 잡히지만 진짜 결함)를 최소 범위로 수정. Performance는 이미 충족이라 추가 변경 없음.

### 변경 1 — 접근성: `/demo` STEP0 옵션 동의 문구 명도 대비

- **파일**: `app/globals.css`
- **변경**: `.check-row.optional { opacity: .82; }` → `.check-row.optional { opacity: .92; }` (주석으로 근거 기록)
- **테스트**: `wrangler dev` 재기동 후 단일 페이지 Lighthouse 재측정 → `color-contrast` 감사 score 1(PASS), `/demo?sample=1` Accessibility 96→**100**. Playwright axe(`accessibility.spec.ts`, light+dark 테마) 재실행 → 2/2 통과.
- **KEEP**.

### 변경 2 — SEO: `/demo` canonical 자기 참조

- **파일**: `app/demo/layout.tsx`(신규, 서버 컴포넌트) — `metadata.alternates.canonical = "/demo"`
- **1차 시도**: SEO 팀원의 부가 제안대로 `robots: { index: false, follow: false }`도 함께 추가.
- **재측정 결과 REVERT**: Lighthouse `is-crawlable` 감사가 noindex 페이지를 자동 감점 처리해 `/demo?sample=1`의 SEO 점수가 100→**66**으로 회귀(증거: `demo-after-fix-check.json`). `/demo?sample=1`은 이번 목표가 명시적으로 요구하는 SEO≥95 측정 대상 페이지이므로, 회귀를 감수하면서까지 밀어붙이지 않고 `robots` 지시자만 되돌림(canonical 수정은 유지). "변경마다 재측정하여 회귀하면 REVERT" 원칙을 그대로 적용한 사례.
- **최종 변경**: canonical만 `/demo`로 수정, robots는 원래 상태(명시적 지시자 없음, 상속 없음) 유지.
- **테스트**: 재빌드 후 `curl`로 `<link rel="canonical" href=".../demo">` 확인(홈이 아님), `<meta name="robots">` 태그 없음(noindex 아님) 확인. Lighthouse 재측정에서 SEO 100 유지 확인.
- **KEEP**(canonical만), **REVERT**(robots noindex).

### 변경 3 — 테스트 커버리지: STEP0 axe 스캔 추가

- **파일**: `tests/e2e/accessibility.spec.ts`
- **변경**: `PATHS` 배열에 `"/demo?sample=1"` 추가(기존 `"/demo"`는 게이트 화면만 스캔하던 것을 보완)
- **효과**: 이번 명도 대비 버그처럼, STEP0 상태에서만 존재하는 요소가 axe 스캔 없이 통과되던 구조적 사각지대를 닫음. 이번 회귀 검증 자체에서 실제로 2/2(light/dark) 통과 확인.
- **KEEP**.

## 최종 재측정 (Loop 3)

수정 3건 반영 후 재빌드 → `wrangler dev` 재기동 → 4페이지×3회 최종 측정(Lighthouse CLI, mobile, `wrangler dev:3001` 기준).

| 페이지 | Performance(3회) | 중앙값 | Accessibility | Best Practices | SEO | LCP | CLS | TBT |
|---|---|---|---|---|---|---|---|---|
| `/` | 99,97,98 | **98** | **100** | 96 | 100 | 1816ms | 0 | 63ms |
| `/demo?sample=1` | 96,97,97 | **97** | **100**(96→100) | 96 | 100 | 2146ms | 0 | 51ms |
| `/how-it-works` | 98,99,99 | **99** | 100 | 96 | 100 | 1785ms | 0 | 33ms |
| `/technology` | 99,98,99 | **99** | 100 | 96 | 100 | 1767ms | 0 | 14ms |

**전 페이지 전 지표 기준 충족.** 가장 효과가 컸던 변경: 측정 서버를 `vinext start`→`wrangler dev`로 바꾼 것(Performance 84~90 → 97~99, 압축 여부가 지배적 요인). 되돌린 변경: `/demo` robots noindex(SEO 회귀로 즉시 REVERT).

## 전체 테스트

- `npm test` → **46/46 통과**(build 포함).
- `npm run build` → 성공(위 재측정 과정에서 여러 차례 확인).
- `npm run lint` → 신규 오류 0(기존 4건은 이전 세션에 이미 main 사전 오류로 확인, 무관 파일).
- `npx playwright test`(chromium+firefox+webkit, 189 tests) → **188 PASS + 1건 Firefox 일시 실패**. 원인 조사: 실패 시점에 Lighthouse(헤드리스 Chrome 다수) 프로세스가 동시 실행 중이었음 — 리소스 경합 가능성을 의심하고 해당 파일만 격리 재실행(`--project=firefox tests/e2e/role-personalization.spec.ts`) → **4/4 전부 통과**. 코드 회귀가 아니라 동시 실행 환경 문제로 결론(추측이 아니라 재현 테스트로 확인). 실패를 숨기지 않고 원인과 함께 기록.
- axe(wcag2a/aa) serious/critical: 0(신규 STEP0 커버리지 포함).
- 320/375/430/768/1440px 가로스크롤·겹침·클리핑 0(기존 `responsive.spec.ts`/`role-personalization.spec.ts`/`pre-merge-verification.spec.ts`가 이미 커버, 이번 변경으로 회귀 없음 — 위 전체 스위트 결과에 포함).
- console/hydration error 0(`home.spec.ts` 등 기존 커버리지, 회귀 없음).
- STEP3→4→5 목표직무 일관성: `role-personalization.spec.ts` 4개 시나리오 전부 통과(격리 재실행 포함).
- 외부 API 실제 요청 0 / Solar 실제 호출 0 / Supabase 변경 0: 이번 변경은 CSS·metadata·테스트 파일뿐이라 해당 경로 자체를 건드리지 않음(기존 검증된 상태 유지).

## 독립 리뷰 (최종, 구현 후 재검토)

- **Lead(Performance Engineer 역할)**: 측정 방법론 교정이 정당함을 재확인, 코드 변경 불필요 판단 유지. **KEEP**.
- **SEO Specialist**: canonical 수정이 실제로 반영됐는지 직접 확인 필요 — (아래 Copywriting 리뷰와 함께 최종 파일 상태로 재확인, canonical `/demo` 자기 참조 확인됨). **KEEP**.
- **Accessibility Auditor**: 명도 대비 수정으로 실제 통과 확인, 커버리지 갭도 함께 닫힘. **KEEP**.
- **Copywriting/Product Marketing Reviewer**: 세 변경 파일(`globals.css` 불투명도 값, `demo/layout.tsx` metadata 전용, `accessibility.spec.ts` 테스트 경로) 전부 직접 읽고 확인 — 사용자에게 보이는 카피·헤드라인·CTA·가치제안 문구는 단 한 글자도 변경되지 않음. **KEEP**.

**NEEDS REWORK 0건.**

## 남은 P1 / 후속 권장 (이번 PR 범위 아님)

1. **로컬 성능 테스트 시 `wrangler dev` 사용을 명문화**: `vinext start`는 정적 자산을 압축하지 않아 성능 점수를 실제보다 낮게 보이게 한다. 팀 문서(runbook 등)에 짧은 안내를 추가하는 것을 권장(이번 PR에서는 devlog로만 기록, 별도 문서 추가는 하지 않음 — 최소 변경 원칙).
2. 기존 P1(이전 devlog에 기록됨, 변경 없음): STEP4 역할 선택기 시각적 라벨 부재, 학습확인 퀴즈 Lv.2 증거등급 로직 재검토.
3. Best Practices가 4페이지 모두 96에 고정(100 아님) — 목표(≥95)는 충족하나, 100에 근접시키고 싶다면 어떤 감사가 −4를 차지하는지 별도 조사 필요(이번 목표 범위 밖).
4. brotli 미검증: 로컬 `wrangler dev`는 gzip만 확인됨. 실제 배포 후 brotli 협상 여부는 이번 작업에서 검증하지 않음(운영 배포 자체가 금지 범위).

## Git/PR

- 브랜치: `perf/lighthouse-90`(`origin/main` `644f8c8`에서 생성).
- 커밋: `perf: raise core Lighthouse scores above 90`(`8178cf3`) — 측정 방법론 교정 문서화 + 접근성 명도 대비 수정 + SEO canonical 수정 + axe 커버리지 확장.
- main 직접 커밋 없음, force-push/reset --hard/브랜치 삭제 없음, Cloudflare 배포·Supabase 변경·secret/binding/DNS 변경 없음.
- PR #78 main 대상 Draft로 생성.

---

## PR #78 병합 전 최종 검증 (Ready for review 게이트)

- 목표: 모든 게이트 통과 시 Draft → Ready for review. 병합·배포 없음. 최대 6턴.
- 시작 상태 재확인: HEAD `8178cf3`(예상과 일치), `origin/main` `644f8c8`(예상과 일치), PR #78 OPEN·Draft·MERGEABLE, 변경 파일 4개 일치. 세션 중 환경이 한 차례 재시작됐으나(다수 MCP 서버 재연결 알림) git 상태·PR 상태는 영향받지 않음을 재확인 후 진행.
- 검증 전 잔여 프로세스 정리: `wrangler dev`·`vinext dev`·`vinext start`·`lighthouse` 프로세스 없음 확인, 이전 세션의 유휴 Playwright test-server(PID 58436)만 추가 종료(무관한 다른 프로젝트 프로세스는 건드리지 않음).

### 순차 검증(Lighthouse·Playwright 동시 실행 금지)

1. `npm test` → 46/46 통과(build 포함). `npm run lint` → 신규 오류 0(기존 4건과 구분 확인).
2. **Playwright 전체(3브라우저, workers=1 기본 설정 그대로)** → 첫 시도는 백그라운드 실행 중 환경 재시작으로 결과 유실(완료 기록 없음 확인 후 결과를 신뢰하지 않고 처음부터 재실행) → 재실행 결과 **189/189 전부 통과**(이전에 실패했던 Firefox 테스트 포함, 다른 프로세스 동시 실행 없이 단독 실행).
3. **Firefox 이전 실패 테스트 격리 5회 반복**(`--project=firefox -g "..." --repeat-each=5`, 다른 프로세스 없이 단독 실행) → **5/5 전부 통과**. 추정이 아니라 실측으로 확인.
4. **접근성**: `wrangler dev` 재기동(Playwright 종료 후 단독 실행) → `accessibility.spec.ts`를 `PLAYWRIGHT_BASE_URL=http://localhost:3001`로 실행 → `/`, `/demo`, `/demo?sample=1`, `/why`, `/how-it-works` × light/dark **10/10 전부 통과**, serious/critical 0.
5. **SEO canonical**: `curl`로 직접 확인 — `/` → `canonical="https://gapproof.forblune.com/"`, `/demo` → `canonical=".../demo"`, `/demo?sample=1` → `canonical=".../demo"`(쿼리스트링 무관하게 동일 canonical로 정규화 확인). `<meta name="robots">` 태그 어디에도 없음(noindex 되돌림이 그대로 유지됨). title·description 회귀 없음.
6. **Lighthouse 최종**(`wrangler dev` 단독, Playwright 종료 후 실행, mobile, 4페이지×3회):

| 페이지 | Performance(3회) | 중앙값 | Accessibility | Best Practices | SEO |
|---|---|---|---|---|---|
| `/` | 99,99,99 | **99** | 100 | 96 | 100 |
| `/demo?sample=1` | 98,98,98 | **98** | 100 | 96 | 100 |
| `/how-it-works` | 99,99,100 | **99** | 100 | 96 | 100 |
| `/technology` | 100,99,99 | **99** | 100 | 96 | 100 |

전 페이지 전 지표 기준 초과 충족(Performance ≥90, 나머지 ≥95). 측정 명령: `npx lighthouse "http://localhost:3001<path>" --output=json --chrome-flags="--headless=new --no-sandbox --disable-gpu" --only-categories=performance,accessibility,best-practices,seo`(mobile은 lighthouse CLI 기본값), 서버는 `npx wrangler dev --port 3001`(`dist/` 빌드 산출물을 실제 workerd 런타임으로 구동) 단독 실행.

### 독립 재검토 (최종 게이트, 4명 병렬)

- **Performance Engineer(Lead 역할)**: Firefox 5회 격리 반복이 "리소스 경합 가설을 반증하지 않았다"는 근거로 유의미하다고 확인(다만 완전한 증명은 아니라는 정직한 단서 포함). 3차례의 `wrangler dev` 측정이 97~100 좁은 범위로 일관되게 수렴함을 근거로 방법론 신뢰도 재확인. **KEEP**.
- **Accessibility Auditor**: `app/globals.css:87`의 `.92` 값과 `accessibility.spec.ts`의 `/demo?sample=1` 경로 추가를 직접 코드에서 재확인(설명과 실제 일치). axe와 Lighthouse가 유사한 대비 알고리즘을 쓰지만 완전히 동일하진 않아 "낮지만 0은 아닌" 잔여 위험을 정직하게 명시. **KEEP**.
- **SEO Specialist**: `app/demo/layout.tsx`가 canonical만 export하고 robots는 없음을 직접 확인. noindex를 "시도 후 되돌림"이 "애초에 안 함"보다 더 신중한 결정이라고 평가. 후속 인덱싱 제외가 필요하면 `robots.txt` disallow가 Lighthouse 감점 없이 가능한 대안임을 제안(비차단). **KEEP WITH MINOR NOTES**.
- **Engineering Code Reviewer**: `git diff main...HEAD`가 정확히 4개 파일과 일치, 시크릿·설정·무관 코드 유입 0, 여러 세션에 걸친 백그라운드 프로세스(wrangler dev·vinext·lighthouse)의 잔재(로그 파일·dist 추적 등)가 git에 전혀 섞이지 않았음을 확인. **`app/demo/layout.tsx` 주석에 로컬 파일 경로(사용자명 포함)가 남아있는 사소한 문제를 발견** → 즉시 수정(경로 제거, "devlog 참고"로 대체). **KEEP WITH MINOR NOTES** → 지적사항 반영 완료.

**NEEDS REWORK 0건.**

### 최종 정리

- 주석 정리(`app/demo/layout.tsx`) 반영 — 코드 동작 변경 없음(주석만 수정이라 재빌드·재테스트 불필요, `git diff`로 comment-only 변경 확인).
- 모든 완료 조건 충족 확인 → PR #78 **Draft → Ready for review** 전환.

### Git/PR

- 커밋: `perf: raise core Lighthouse scores above 90`(`8178cf3`) + 후속 주석 정리 커밋.
- main 직접 커밋 없음, 병합 없음, 운영 배포 없음, Cloudflare/Supabase/secret/binding/DNS 변경 없음, force-push/reset --hard/브랜치 삭제 없음.
